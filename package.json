{
  "name": "konus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Konuş — a fun, efficient PWA for learning conversational Turkish (English speakers).",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "build:deck": "node scripts/build-deck.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
