// @ts-check
/** Human-visible build marker. Bump on each deploy so stale caches are obvious
 *  (shown in Settings and logged to the console at startup). */
export const APP_VERSION = '0.6.0';
export const BUILD = '2026-07-25-e';
