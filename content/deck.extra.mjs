// @ts-check
/**
 * Generated extra units appended after the hand-authored core deck
 * (`deck.source.mjs`). Produced by the `expand-turkish-deck` workflow (parallel
 * themed generation + per-unit native-level verification), then deduped against
 * the core headwords. Same shape as `deck.source.mjs` units so `build-deck.mjs`
 * processes them identically.
 *
 * Populated by the assembly step; empty until then.
 */

/** @type {{ id: string, title: string, entries: any[] }[]} */
export const units = [];
